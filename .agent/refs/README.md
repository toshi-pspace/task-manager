# refs/

Drop reference **files** here for agents to consult: brand guidelines (PDF),
API specs (JSON), screenshots, client briefs, data samples, etc.

After adding a file, mention it in `../CONTEXT.md` under "Reference material" —
agents read CONTEXT.md reliably but won't always scan this folder on their own.
