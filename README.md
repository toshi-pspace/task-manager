# <PROJECT NAME>

<One-line description.>

## Status
See `.agent/STATE.md` for current state and next steps.

## Run it
```
<commands>
```

## Layout
- `.agent/` — agent memory (state, decisions, context, refs). Read this to resume work.
- `docs/`   — the wiki: architecture, guides, notes (human knowledge).
- `src/`    — source code.
- `assets/` — `raw/` master files (.blend, .psd) → `exports/` generated output.
- `scratch/`— throwaway experiments.
- `archive/`— old versions kept for reference.

Agent instruction pointers: `CLAUDE.md`, `AGENTS.md`, `replit.md` (all point at `.agent/`).
